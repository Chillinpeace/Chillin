(() => {
  'use strict';
  const API='/api';
  const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));
  const money=v=>`₹${Number(v||0).toLocaleString('en-IN')}`;
  const date=v=>v?new Date(`${String(v).slice(0,10)}T00:00:00`).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'}):'-';
  const api=async(path,opt={})=>{const r=await fetch(API+path,{credentials:'include',headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});const d=await r.json().catch(()=>null);if(!r.ok)throw Error(d?.error||`Request failed (${r.status})`);return d;};
  let root,content,active='automation';
  const tabs=[['automation','Automation'],['finance-ai','Financial Intelligence'],['operations','Operations'],['team-security','Team & Security'],['system','System Health']];
  const style=`<style id="p59-style">#p59-wrap{font-size:12px}.p59-actions{display:flex;gap:8px;flex-wrap:wrap;margin:0 0 14px}.p59-actions input,.p59-actions select,.p59-form input,.p59-form select,.p59-form textarea{border:1px solid rgba(255,255,255,.1);border-radius:10px;padding:9px 10px;background:#0e1723;color:#f8fafc;outline:none}.p59-form{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:10px;margin-bottom:14px}.p59-form label{display:flex;flex-direction:column;gap:5px;color:#8e99aa;font-size:10px}.p59-form textarea{min-height:70px;resize:vertical}.p59-btn{border:0;border-radius:10px;padding:9px 12px;background:#10b981;color:#04130d;font-weight:800;cursor:pointer}.p59-btn.alt{background:#172131;color:#dce3ec}.p59-btn.danger{background:#7f1d1d;color:#fff}.p59-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(165px,1fr));gap:10px;margin-bottom:14px}.p59-card{background:#0e1723;border:1px solid rgba(255,255,255,.08);border-radius:15px;padding:14px}.p59-card h3{margin:0 0 6px;color:#8e99aa;font-size:10px}.p59-big{font-size:22px;font-weight:850}.p59-table{width:100%;border-collapse:collapse;min-width:760px}.p59-table th,.p59-table td{padding:9px;border-bottom:1px solid rgba(255,255,255,.06);text-align:left;font-size:11px;vertical-align:top}.p59-table th{color:#8e99aa;background:#101a27}.p59-table-wrap{overflow:auto;border:1px solid rgba(255,255,255,.07);border-radius:14px}.p59-status{padding:4px 7px;border-radius:999px;background:#172131}.p59-ok{color:#34d399}.p59-warn{color:#fbbf24}.p59-bad{color:#f87171}.p59-note{padding:11px;border-radius:12px;background:rgba(16,185,129,.08);border:1px solid rgba(16,185,129,.15);color:#9de8ca;margin-bottom:12px}.p59-error{padding:12px;border-radius:12px;background:#35151a;color:#fecaca;margin-bottom:12px}@media(max-width:700px){.p59-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.p59-form{grid-template-columns:1fr}}
</style>`;
  const button=(k,l)=>`<button class="p59-tab" data-p59="${k}">${l}</button>`;
  function nav(){let n=root.querySelector('.p4-nav');if(!n||n.dataset.p59==='1')return;tabs.forEach(([k,l])=>{const b=document.createElement('button');b.textContent=l;b.dataset.p59=k;b.className='p59-tab';b.onclick=()=>show(k);n.appendChild(b);});n.dataset.p59='1';}
  function setup(){root=document.getElementById('peacely-phase4-root');if(!root)return false;content=root.querySelector('.p4-content');if(!content)return false;if(!document.getElementById('p59-style'))document.head.insertAdjacentHTML('beforeend',style);nav();return true;}
  function mark(k){root.querySelectorAll('.p59-tab').forEach(b=>b.classList.toggle('active',b.dataset.p59===k));}
  async function show(k){if(!setup())return;active=k;mark(k);content.innerHTML='<h1>Loading…</h1>';try{if(k==='automation')await automation();else if(k==='finance-ai')await finance();else if(k==='operations')await operations();else if(k==='team-security')await team();else await system();}catch(e){content.innerHTML=`<h1>${esc(k)}</h1><div class="p59-error">${esc(e.message)}</div>`;}}
  async function automation(){const [s,d,n]=await Promise.all([api('/automation/settings'),api('/automation/due'),api('/notifications')]);content.innerHTML=`<h1>Automation & Communication</h1><div class="p59-note">Automated reminders are safely queued into a notification history. WhatsApp provider delivery can be connected later without changing tenant or invoice data.</div><div class="p59-grid"><div class="p59-card"><h3>Due Soon</h3><div class="p59-big">${d.due.length}</div></div><div class="p59-card"><h3>Overdue</h3><div class="p59-big">${d.overdue.length}</div></div><div class="p59-card"><h3>Notification Log</h3><div class="p59-big">${n.length}</div></div></div><div class="p59-form"><label>Reminders Enabled<select id="p59-rem"><option value="true" ${s.reminders_enabled?'selected':''}>Yes</option><option value="false" ${!s.reminders_enabled?'selected':''}>No</option></select></label><label>Days Before Due<input id="p59-days" type="number" min="0" max="30" value="${s.reminder_days_before}"></label><label>Overdue Reminders<select id="p59-over"><option value="true" ${s.overdue_reminders_enabled?'selected':''}>Yes</option><option value="false" ${!s.overdue_reminders_enabled?'selected':''}>No</option></select></label><label>Recurring Invoice Engine<select id="p59-rec"><option value="false" ${!s.recurring_invoices_enabled?'selected':''}>Off</option><option value="true" ${s.recurring_invoices_enabled?'selected':''}>On</option></select></label></div><div class="p59-actions"><button class="p59-btn" id="p59-save-auto">Save automation</button><button class="p59-btn alt" id="p59-refresh-auto">Refresh</button></div><h3>Upcoming / Overdue Reminders</h3><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Tenant</th><th>Invoice</th><th>Due</th><th>Amount</th><th>Status</th><th>Action</th></tr></thead><tbody>${[...d.due,...d.overdue].map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.invoice_number)}</td><td>${date(x.due_date)}</td><td>${money(x.amount)}</td><td>${esc(x.status)}</td><td><button class="p59-btn alt" data-reminder="${x.tenant_id}" data-invoice="${x.invoice_id}" data-phone="${esc(x.phone||'')}">Queue</button></td></tr>`).join('')||'<tr><td colspan="6">No reminders due.</td></tr>'}</tbody></table></div><h3 style="margin-top:18px">Notification History</h3><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Created</th><th>Tenant</th><th>Channel</th><th>Status</th><th>Message</th></tr></thead><tbody>${n.map(x=>`<tr><td>${date(x.created_at)}</td><td>${esc(x.tenant_name||'-')}</td><td>${esc(x.channel)}</td><td>${esc(x.status)}</td><td>${esc(x.message)}</td></tr>`).join('')||'<tr><td colspan="5">No notification history.</td></tr>'}</tbody></table></div>`;
    document.getElementById('p59-save-auto').onclick=async()=>{await api('/automation/settings',{method:'PUT',body:JSON.stringify({reminders_enabled:document.getElementById('p59-rem').value==='true',reminder_days_before:Number(document.getElementById('p59-days').value),overdue_reminders_enabled:document.getElementById('p59-over').value==='true',recurring_invoices_enabled:document.getElementById('p59-rec').value==='true'})});show('automation');};document.getElementById('p59-refresh-auto').onclick=()=>show('automation');root.querySelectorAll('[data-reminder]').forEach(b=>b.onclick=async()=>{const msg=`Hello, this is a rent reminder from Peacely. Invoice ${b.dataset.invoice} is due/overdue. Please arrange payment. Thank you.`;await api('/notifications/queue',{method:'POST',body:JSON.stringify({tenant_id:Number(b.dataset.reminder),invoice_id:Number(b.dataset.invoice),channel:'whatsapp',type:'rent_reminder',recipient:b.dataset.phone,message:msg})});show('automation');});
  }
  async function finance(){const now=new Date(),from=`${now.getFullYear()}-01-01`,to=now.toISOString().slice(0,10);const [r,a,p]=await Promise.all([api(`/reports/financial?from=${from}&to=${to}`),api('/reports/aging'),api('/reports/property-profitability')]);content.innerHTML=`<h1>Financial Intelligence</h1><div class="p59-grid">${[['Invoiced',money(r.invoiced)],['Collected',money(r.collected)],['Outstanding',money(r.outstanding)],['Expenses',money(r.expenses)],['Net Cash',money(r.net_cash)],['Collection Rate',`${r.invoiced?Math.round(r.collected/r.invoiced*100):0}%`]].map(x=>`<div class="p59-card"><h3>${x[0]}</h3><div class="p59-big">${x[1]}</div></div>`).join('')}</div><h3>Outstanding Rent Ageing</h3><div class="p59-grid">${Object.entries(a.buckets).map(([k,v])=>`<div class="p59-card"><h3>${k.replace('_','-')}</h3><div class="p59-big">${money(v)}</div></div>`).join('')}</div><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Tenant</th><th>Property</th><th>Invoice</th><th>Due</th><th>Days</th><th>Balance</th></tr></thead><tbody>${a.items.map(x=>`<tr><td>${esc(x.tenant_name)}</td><td>${esc(x.property_name)}</td><td>${esc(x.invoice_number)}</td><td>${date(x.due_date)}</td><td>${x.days_overdue}</td><td>${money(x.balance)}</td></tr>`).join('')||'<tr><td colspan="6">No outstanding balances.</td></tr>'}</tbody></table></div><h3 style="margin-top:18px">Property Profitability</h3><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Property</th><th>Active</th><th>Beds</th><th>Occupied</th><th>Income</th><th>Expenses</th><th>Profit</th></tr></thead><tbody>${p.map(x=>`<tr><td>${esc(x.name)}</td><td>${x.active_tenants}</td><td>${x.beds}</td><td>${x.occupied}</td><td>${money(x.income)}</td><td>${money(x.expenses)}</td><td>${money(x.profit)}</td></tr>`).join('')}</tbody></table></div><h3 style="margin-top:18px">Monthly P&L</h3><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Month</th><th>Invoiced</th><th>Collected</th><th>Expenses</th><th>Net Cash</th></tr></thead><tbody>${r.monthly.map(x=>`<tr><td>${x.month}</td><td>${money(x.invoiced)}</td><td>${money(x.collected)}</td><td>${money(x.expenses)}</td><td>${money(Number(x.collected)-Number(x.expenses))}</td></tr>`).join('')}</tbody></table></div>`;}
  async function operations(){const [s,t]=await Promise.all([api('/operations/summary'),api('/maintenance')]);content.innerHTML=`<h1>Property Operations</h1><div class="p59-grid">${[['Open Tickets',s.tickets.open],['Critical Tickets',s.tickets.critical],['Maintenance Cost',money(s.tickets.cost)],['Occupied Beds',`${s.beds.occupied}/${s.beds.total}`],['Active Tenants',s.tenants.active]].map(x=>`<div class="p59-card"><h3>${x[0]}</h3><div class="p59-big">${x[1]}</div></div>`).join('')}</div><div class="p59-actions"><button class="p59-btn" id="p59-new-ticket">+ Maintenance Ticket</button><button class="p59-btn alt" id="p59-refresh-op">Refresh</button></div><div id="p59-ticket-form"></div><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Title</th><th>Property</th><th>Tenant</th><th>Priority</th><th>Status</th><th>Cost</th><th>Due</th><th></th></tr></thead><tbody>${t.map(x=>`<tr><td><strong>${esc(x.title)}</strong><div>${esc(x.description||'')}</div></td><td>${esc(x.property_name||'-')}<br>${esc(x.room_number||'-')} / ${esc(x.bed_number||'-')}</td><td>${esc(x.tenant_name||'-')}</td><td>${esc(x.priority)}</td><td>${esc(x.status)}</td><td>${money(x.actual_cost)}</td><td>${date(x.due_date)}</td><td><button class="p59-btn alt" data-resolve="${x.id}">Resolve</button></td></tr>`).join('')||'<tr><td colspan="8">No maintenance tickets.</td></tr>'}</tbody></table></div>`;document.getElementById('p59-refresh-op').onclick=()=>show('operations');document.getElementById('p59-new-ticket').onclick=()=>{document.getElementById('p59-ticket-form').innerHTML=`<div class="p59-form"><label>Title<input id="mt-title"></label><label>Property ID<input id="mt-property" type="number"></label><label>Tenant ID<input id="mt-tenant" type="number"></label><label>Priority<select id="mt-priority"><option>Medium</option><option>High</option><option>Urgent</option><option>Low</option></select></label><label>Due Date<input id="mt-due" type="date"></label><label>Estimated Cost<input id="mt-est" type="number" min="0"></label><label>Description<textarea id="mt-desc"></textarea></label></div><button class="p59-btn" id="mt-save">Save Ticket</button>`;document.getElementById('mt-save').onclick=async()=>{await api('/maintenance',{method:'POST',body:JSON.stringify({title:document.getElementById('mt-title').value,property_id:Number(document.getElementById('mt-property').value)||null,tenant_id:Number(document.getElementById('mt-tenant').value)||null,priority:document.getElementById('mt-priority').value,due_date:document.getElementById('mt-due').value,estimated_cost:Number(document.getElementById('mt-est').value)||0,description:document.getElementById('mt-desc').value})});show('operations');};};root.querySelectorAll('[data-resolve]').forEach(b=>b.onclick=async()=>{const id=b.dataset.resolve;await api(`/maintenance/${id}`,{method:'PUT',body:JSON.stringify({status:'Resolved',title:'',description:'',category:'General',priority:'Medium',assigned_to:'',estimated_cost:0,actual_cost:0})});show('operations');});}
  async function team(){const users=await api('/team');content.innerHTML=`<h1>Team & Security</h1><div class="p59-note">Owner-scoped staff accounts, roles, property restrictions and audit history are stored separately from the primary owner login. The existing owner login remains unchanged.</div><div class="p59-actions"><button class="p59-btn" id="p59-add-user">+ Staff Account</button><button class="p59-btn alt" id="p59-audit">View Audit Log</button></div><div id="p59-team-form"></div><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Active</th><th>Property Scope</th><th>Created</th><th></th></tr></thead><tbody>${users.map(u=>`<tr><td>${esc(u.name)}</td><td>${esc(u.email)}</td><td>${esc(u.role)}</td><td>${u.active?'Yes':'No'}</td><td>${Array.isArray(u.property_ids)&&u.property_ids.length?u.property_ids.join(', '):'All / unrestricted'}</td><td>${date(u.created_at)}</td><td><button class="p59-btn alt" data-toggle-user="${u.id}" data-active="${u.active}">${u.active?'Disable':'Enable'}</button></td></tr>`).join('')||'<tr><td colspan="7">No staff accounts yet.</td></tr>'}</tbody></table></div><h3 style="margin-top:18px">Roles</h3><div class="p59-grid"><div class="p59-card"><h3>Manager</h3><div>Operational + financial access</div></div><div class="p59-card"><h3>Staff</h3><div>Configurable permissions</div></div><div class="p59-card"><h3>Viewer</h3><div>Read-only operational access</div></div></div>`;document.getElementById('p59-add-user').onclick=()=>{document.getElementById('p59-team-form').innerHTML=`<div class="p59-form"><label>Name<input id="tu-name"></label><label>Email<input id="tu-email" type="email"></label><label>Password<input id="tu-pass" type="password" minlength="6"></label><label>Role<select id="tu-role"><option>manager</option><option>staff</option><option>viewer</option></select></label></div><button class="p59-btn" id="tu-save">Create</button>`;document.getElementById('tu-save').onclick=async()=>{await api('/team',{method:'POST',body:JSON.stringify({name:document.getElementById('tu-name').value,email:document.getElementById('tu-email').value,password:document.getElementById('tu-pass').value,role:document.getElementById('tu-role').value})});show('team-security');};};root.querySelectorAll('[data-toggle-user]').forEach(b=>b.onclick=async()=>{await api(`/team/${b.dataset.toggleUser}`,{method:'PUT',body:JSON.stringify({active:b.dataset.active!=='true'})});show('team-security');});document.getElementById('p59-audit').onclick=async()=>{const a=await api('/audit?limit=100');document.getElementById('p59-team-form').innerHTML=`<h3>Audit Log</h3><div class="p59-table-wrap"><table class="p59-table"><thead><tr><th>Time</th><th>Action</th><th>Entity</th><th>ID</th><th>Details</th></tr></thead><tbody>${a.map(x=>`<tr><td>${esc(x.created_at)}</td><td>${esc(x.action)}</td><td>${esc(x.entity_type||'-')}</td><td>${esc(x.entity_id||'-')}</td><td>${esc(JSON.stringify(x.details))}</td></tr>`).join('')}</tbody></table></div>`;};}
  async function system(){const [h,m,c]=await Promise.all([api('/system/health'),api('/system/metrics'),api('/system/checks')]);content.innerHTML=`<h1>Production & Scale</h1><div class="p59-grid"><div class="p59-card"><h3>API Status</h3><div class="p59-big p59-ok">${esc(h.status)}</div></div><div class="p59-card"><h3>DB Latency</h3><div class="p59-big">${h.latency_ms} ms</div></div><div class="p59-card"><h3>Uptime</h3><div class="p59-big">${Math.floor(h.uptime_seconds/3600)}h</div></div><div class="p59-card"><h3>Properties</h3><div class="p59-big">${m.metrics.properties}</div></div><div class="p59-card"><h3>Tenants</h3><div class="p59-big">${m.metrics.tenants}</div></div><div class="p59-card"><h3>Maintenance</h3><div class="p59-big">${m.metrics.maintenance}</div></div></div><div class="p59-actions"><button class="p59-btn" id="p59-backup">Download Full JSON Backup</button><button class="p59-btn alt" id="p59-refresh-system">Run Checks Again</button></div><h3>Production Checks</h3><ul class="p4-checklist">${c.checks.map(x=>`<li><span class="${x.status==='ok'?'p59-ok':'p59-bad'}">● ${esc(x.name)}</span> — ${esc(x.status)} — ${x.latency_ms} ms</li>`).join('')}</ul><h3 style="margin-top:18px">Runtime</h3><div class="p59-note">Node ${esc(h.node)} · process uptime ${Math.floor(h.uptime_seconds)} seconds · server-backed health checks and owner-scoped backup are enabled.</div>`;document.getElementById('p59-refresh-system').onclick=()=>show('system');document.getElementById('p59-backup').onclick=async()=>{const d=await api('/system/backup');const blob=new Blob([JSON.stringify(d,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`peacely-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href);};}
  const timer=setInterval(()=>{if(setup()){clearInterval(timer);show(active);}},500);
})();
